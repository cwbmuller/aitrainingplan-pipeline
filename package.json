{
  "name": "aitrainingplan-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generate -> certify -> repair -> judge -> publish pipeline for AI training plans. Zero dependencies, Node 22.",
  "scripts": {
    "run": "node src/run.mjs",
    "run:all": "node src/run.mjs --all",
    "certify": "node src/run.mjs --certify-only"
  },
  "engines": { "node": ">=22" }
}
